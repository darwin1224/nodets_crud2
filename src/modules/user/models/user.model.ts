import { Schema } from 'mongoose'

export const User = new Schema({
  nama_user: {
    type: String,
    required: true
  },
  username: {
    type: String,
    required: true
  },
  password: {
    type: String,
    required: true
  }
})